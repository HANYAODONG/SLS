# 项目交接与后续训练指南

本文档用于在更换服务器、重新配置环境、开启新对话或交接给队友时，快速恢复对整个项目的上下文理解。它从项目目标、目录结构、已完成工作、数据与权重、复现流程、创新实验线、训练操作、训练后权重使用、评测和回滚方案几个方面完整说明当前项目状态。

## 一、项目总体目标

本项目基于开源仓库：

```text
SLSforASVspoof-2021-DF
```

当前项目目标分为三层：

1. **原 SLS 主模型复现**

   使用原始 SLS 模型结构、XLS-R 300M 前端和原始权重 `MMpaper_model.pth`，完成 ASVspoof 2021 DF 与 In-the-Wild 数据集评测。

2. **不改变主模型的横向扩展**

   围绕音频深度伪造检测任务，扩展网页助手、风险报告、视频音画分离、ASR、声纹、VLM 审计、时间戳存证等工程能力。

3. **模型结构创新实验**

   参考三个方案文档，新增 hybrid 实验线，实现：

   - Mean+Std 统计感知 SLS；
   - SwiGLU 特征门控；
   - 普通时间注意力池化；
   - 通道引导时间注意力池化。

   该实验线不会覆盖原项目文件。它需要重新训练才能得到有效结果。

4. **零训练可解释性报告**

   参考 `XLS-R_SLS_大模型可解释性零训练可执行方案(1).md`，新增 `explainability/` 模块和 `scripts/explain_audio.py`，在不修改原模型、不重新训练的前提下生成结构化证据和中文模板报告。

## 二、当前最重要的结论

### 2.1 原主线已经复现成功

原主线入口：

```text
main.py
model.py
```

原主线权重：

```text
MMpaper_model.pth
```

已完成的主要评测结果：

```text
DF 20000            EER = 1.99%
In-the-Wild 20000   EER ≈ 7.47%
```

这部分是当前稳定版本，可以用于论文/报告中的复现结果。

### 2.2 hybrid 创新线已经完成代码接入，但尚未训练

hybrid 实验线入口：

```text
main_hybrid.py
model_hybrid.py
```

该线新增了模型参数，因此不能直接使用 `MMpaper_model.pth` 作为最终有效权重。必须重新训练生成新的 checkpoint，例如：

```text
models/hybrid_.../best.pth
```

### 2.3 原主线和实验线相互隔离

原项目文件没有被覆盖：

```text
model.py       原 SLS 模型
main.py        原训练/评测入口
```

新增实验文件全部单独存在：

```text
model_hybrid.py
main_hybrid.py
test_hybrid_modules.py
scripts/train_hybrid_*.sh
scripts/eval_hybrid_*.sh
```

如果后续决定不做训练，可以继续使用原主线，不会影响复现结果。

## 三、关键目录结构

### 3.1 主模型与训练入口

```text
model.py                 原始 SLS 模型结构
main.py                  原始 SLS 训练/评测入口
model_hybrid.py          hybrid 创新模型结构
main_hybrid.py           hybrid 训练/评测入口
data_utils_SSL.py        数据读取、音频加载、RawBoost 处理
RawBoost.py              RawBoost 数据增强
```

### 3.2 评测脚本

```text
evaluate_2021_DF.py          DF EER 计算
evaluate_2021_LA.py          LA 评测入口
evaluate_in_the_wild.py      In-the-Wild EER 计算
eval_metrics_DF.py
eval_metric_LA.py
```

### 3.3 数据协议与 key

```text
database/ASVspoof_DF_cm_protocols/
keys/
```

当前已经整理好的 20000 条子集包括：

```text
database/ASVspoof_DF_cm_protocols/ASVspoof2021.DF.cm.eval.first20000.trl.txt
database/ASVspoof_DF_cm_protocols/in_the_wild.first20000.eval.txt

keys/DF_20000/
keys/Wild_20000/
```

### 3.4 原主线脚本

```text
scripts/eval_df_20000.sh
scripts/eer_df_20000.sh
scripts/eval_wild_20000.sh
scripts/eer_wild_20000.sh
```

### 3.5 hybrid 实验脚本

```text
scripts/test_hybrid_modules.sh
scripts/train_hybrid_stat_sls.sh
scripts/train_hybrid_swiglu.sh
scripts/train_hybrid_temporal.sh
scripts/train_hybrid_full.sh
scripts/eval_hybrid_df_20000.sh
scripts/eval_hybrid_wild_20000.sh
```

### 3.6 横向扩展能力

```text
extension_audit.py
extensions/
analysis/
llm/
web/
web_app.py
```

相关脚本：

```text
scripts/check_extension_deps.sh
scripts/preprocess_video_demo.sh
scripts/certify_file_demo.sh
scripts/risk_wild_20000.sh
scripts/run_llm_web.sh
scripts/test_deepseek_api.sh
```

### 3.7 零训练可解释性

```text
configs/explainability.json
configs/explainability_cpu_debug.json
explainability/
scripts/inspect_model_output.py
scripts/compare_original_output.py
scripts/explain_audio.py
```

用途：

- 导出原模型真实中间形状；
- 检查解释路径与原预测输出一致；
- 生成时间遮挡、频带遮挡、稳定性等结构化证据；
- 生成中文模板解释报告。

## 四、重要文件与用途

### 4.1 文档

```text
README.md
```

给队友部署和跑数据使用，包含环境安装、DF/Wild 20000 评测、扩展功能说明。

```text
实现说明.md
```

记录三份方案文档的采纳情况，以及当前 hybrid 创新线的实现状态。

```text
创新实验复原文档.md
```

说明如果后续不训练，如何回到稳定复现主线。

```text
复现报告.md
```

记录原 SLS 主模型复现过程和结果。

```text
扩展功能依赖与样本清单.md
样本数据收集清单.md
扩展依赖安装与验证清单.md
```

用于扩展功能依赖安装、样本收集和验证。

### 4.2 不应上传 GitHub 的内容

以下内容体积大或包含敏感信息，不应直接上传普通 GitHub 仓库：

```text
.env
venv/
venv_speaker/
MMpaper_model.pth
xlsr2_300m.pt
data/ASVspoof2021_DF_eval/flac/
release_in_the_wild/
samples/
```

真实 API Key 不应写入代码、README 或文档。

## 五、环境配置

### 5.1 推荐 Python 版本

当前本机使用：

```text
Python 3.10
```

原官方项目推荐 Python 3.7，但当前环境已经在 Python 3.10 下跑通。

### 5.2 创建主环境

```bash
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
```

安装 PyTorch：

```bash
pip install torch==1.12.1+cu113 torchvision==0.13.1+cu113 torchaudio==0.12.1+cu113 \
  --extra-index-url https://download.pytorch.org/whl/cu113
```

安装项目依赖：

```bash
pip install -r requirements.txt
```

解压并安装 fairseq：

```bash
unzip -n fairseq-a54021305d6b3c4c5959ac9395135f63202db8f1.zip
pip install --editable ./fairseq-a54021305d6b3c4c5959ac9395135f63202db8f1
```

如果压缩包不存在，但目录 `fairseq-a54021305d6b3c4c5959ac9395135f63202db8f1/` 已存在，可以直接：

```bash
pip install --editable ./fairseq-a54021305d6b3c4c5959ac9395135f63202db8f1
```

### 5.3 环境检查

```bash
source venv/bin/activate
python - <<'PY'
import torch, torchaudio, google.protobuf
print("torch:", torch.__version__)
print("torchaudio:", torchaudio.__version__)
print("protobuf:", google.protobuf.__version__)
print("cuda:", torch.cuda.is_available())
if torch.cuda.is_available():
    print(torch.cuda.get_device_name(0))
PY
```

当前已验证可用的主环境版本：

```text
torch==1.12.1+cu113
torchaudio==0.12.1+cu113
protobuf==3.20.3
```

### 5.4 扩展环境注意事项

WeSpeaker 不建议直接装进主 `venv`，因为它可能升级 `torch/protobuf`，影响原模型复现。当前项目使用独立环境：

```bash
bash scripts/setup_speaker_env.sh
```

检查扩展依赖：

```bash
bash scripts/check_extension_deps.sh
```

`pymilvus` 当前不是必须项，项目已经用 JSONL 方式做声纹注册/匹配替代。

## 六、必须准备的数据和权重

### 6.1 XLS-R 预训练模型

文件名：

```text
xlsr2_300m.pt
```

放置位置：

```text
项目根目录/xlsr2_300m.pt
```

下载命令：

```bash
wget -O xlsr2_300m.pt https://dl.fbaipublicfiles.com/fairseq/wav2vec/xlsr2_300m.pt
```

### 6.2 原 SLS 权重

文件名：

```text
MMpaper_model.pth
```

放置位置：

```text
项目根目录/MMpaper_model.pth
```

用途：

- 只能完整匹配原 `model.py`；
- 用于原主线 DF/Wild 复现；
- 不能作为 hybrid 模型的最终有效权重。

### 6.3 DF 数据集

目录结构：

```text
data/ASVspoof2021_DF_eval/flac/
```

其中存放 `.flac` 音频。

### 6.4 In-the-Wild 数据集

目录结构：

```text
release_in_the_wild/
```

其中存放 In-the-Wild 音频文件。

### 6.5 训练数据

hybrid 训练默认使用 ASVspoof2019 LA train：

```text
data/ASVspoof2019_LA_train/flac/
database/ASVspoof_DF_cm_protocols/ASVspoof2019.LA.cm.train.trn.txt
```

注意：

```text
如果新环境只有 DF eval 和 Wild eval，无法直接训练。
训练至少需要 ASVspoof2019_LA_train/flac/。
```

## 七、原主线复现流程

### 7.1 DF 20000

```bash
source venv/bin/activate
bash scripts/eval_df_20000.sh
bash scripts/eer_df_20000.sh
```

输出：

```text
scores/scores_DF_20000.txt
```

当前结果：

```text
EER = 1.99%
```

### 7.2 In-the-Wild 20000

```bash
source venv/bin/activate
bash scripts/eval_wild_20000.sh
bash scripts/eer_wild_20000.sh
```

输出：

```text
scores/scores_Wild_20000.txt
```

当前结果：

```text
EER ≈ 7.47%
```

### 7.3 原主线 tiny 检查

如果换了新环境，建议先跑 tiny：

```bash
bash scripts/eval_df_tiny10.sh
bash scripts/eer_df_tiny10.sh
```

tiny 用于检查环境、权重、数据路径是否打通，不用于正式结论。

## 八、hybrid 创新模型结构

hybrid 实验线放在：

```text
model_hybrid.py
main_hybrid.py
```

### 8.1 Mean+Std 统计感知 SLS

对应类：

```text
StatisticalSLS
```

逻辑：

```text
XLS-R 24 层隐藏特征 [B, L, T, D]
→ 每层计算 mean/std
→ MLP 预测层权重
→ 对 24 层加权求和
→ 得到 [B, T, D]
```

开关：

```bash
--use_stat_sls 1
--stat_sls_use_std 1
```

### 8.2 SwiGLU 特征门控

对应类：

```text
SwiGLUGate
```

逻辑：

```text
fused [B, T, D]
→ LayerNorm
→ SiLU(Wg x) * Wv x
→ out projection
→ residual
```

开关：

```bash
--use_swiglu 1
```

### 8.3 普通时间注意力池化

对应类：

```text
TemporalAttentionPooling
```

开关：

```bash
--pooling_type temporal
```

### 8.4 通道引导时间注意力

对应类：

```text
ChannelGuidedTemporalAttention
```

开关：

```bash
--pooling_type cgta
--cgta_use_std 1
--cgta_stat_residual 1
```

### 8.5 原 head 保留

如果使用：

```bash
--pooling_type maxpool
```

则走类似原模型的后端：

```text
BatchNorm2d → SELU → max_pool2d → fc1 → fc3
```

这用于消融实验，比如只测试 Mean+Std 或只测试 SwiGLU。

## 九、hybrid 训练前检查

### 9.1 模块测试

先运行：

```bash
source venv/bin/activate
bash scripts/test_hybrid_modules.sh
```

预期输出：

```text
All hybrid module tests passed.
```

该测试不加载 XLS-R，不读数据，只验证新增模块的张量维度和注意力权重是否正常。

### 9.2 单 epoch 试跑

第一次训练不要直接跑 10 epoch，先跑 1 epoch：

```bash
EPOCHS=1 BATCH_SIZE=1 bash scripts/train_hybrid_full.sh
```

观察：

- 是否爆显存；
- 是否能读取训练数据；
- loss 是否正常输出；
- `models/` 下是否生成 checkpoint。

## 十、hybrid 训练脚本

当前准备了四组实验脚本。

### 10.1 只加 Mean+Std SLS

```bash
EPOCHS=10 BATCH_SIZE=1 bash scripts/train_hybrid_stat_sls.sh
```

等价配置：

```text
--use_stat_sls 1
--use_swiglu 0
--pooling_type maxpool
```

用途：

```text
测试统计感知层选择是否有效。
```

### 10.2 只加 SwiGLU

```bash
EPOCHS=10 BATCH_SIZE=1 bash scripts/train_hybrid_swiglu.sh
```

等价配置：

```text
--use_stat_sls 0
--use_swiglu 1
--pooling_type maxpool
```

用途：

```text
测试层融合后特征维度门控是否有效。
```

### 10.3 只加普通时间注意力

```bash
EPOCHS=10 BATCH_SIZE=1 bash scripts/train_hybrid_temporal.sh
```

等价配置：

```text
--use_stat_sls 0
--use_swiglu 0
--pooling_type temporal
```

用途：

```text
测试固定池化替换为时间注意力后是否有效。
```

### 10.4 三个模块全加

```bash
EPOCHS=10 BATCH_SIZE=1 bash scripts/train_hybrid_full.sh
```

等价配置：

```text
--use_stat_sls 1
--use_swiglu 1
--pooling_type cgta
```

用途：

```text
测试完整创新模型。
```

## 十一、训练参数说明

训练脚本支持环境变量覆盖：

```text
EPOCHS                 训练轮数，默认 10
BATCH_SIZE             batch size，默认 1
DATABASE_PATH          数据根目录，默认 data
XLSR_CHECKPOINT        XLS-R checkpoint，默认 xlsr2_300m.pt
NUM_WORKERS            DataLoader worker 数，默认 2
EARLY_STOP_PATIENCE    早停耐心值，默认 3
```

示例：

```bash
EPOCHS=3 BATCH_SIZE=1 NUM_WORKERS=2 bash scripts/train_hybrid_full.sh
```

### 11.1 关于早停

当前 `main_hybrid.py` 使用训练 loss 做简单早停：

```text
如果连续 EARLY_STOP_PATIENCE 个 epoch 训练 loss 没有变好，就提前停止。
```

默认：

```text
EARLY_STOP_PATIENCE=3
```

如果希望完整跑满 epoch，可设置：

```bash
EARLY_STOP_PATIENCE=0 EPOCHS=10 bash scripts/train_hybrid_full.sh
```

### 11.2 关于显存

本机曾使用：

```text
NVIDIA GeForce RTX 3050 Laptop GPU
显存 4GB
```

建议：

```text
BATCH_SIZE=1
EVAL_BATCH_SIZE=1
--disable_cudnn
```

如果换到更大显存服务器，可以尝试：

```bash
BATCH_SIZE=2
BATCH_SIZE=4
```

但每次提高 batch size 后，都建议先跑 1 epoch 检查显存。

## 十二、训练输出和 checkpoint

训练后会在 `models/` 下生成目录，命名类似：

```text
models/hybrid_DF_weighted_CCE_10_1_stat1_swiglu1_poolcgta_full/
```

其中包含：

```text
epoch_0.pth
epoch_1.pth
...
best.pth
```

含义：

- `epoch_*.pth`：每个 epoch 后保存的 checkpoint；
- `best.pth`：当前训练 loss 最低的 checkpoint；
- 正式评测优先使用 `best.pth`。

### 12.1 checkpoint 和原 `.pth` 的关系

`MMpaper_model.pth`：

```text
原 SLS 模型权重，只适配 model.py。
```

`models/.../best.pth`：

```text
hybrid 模型训练出来的新权重，只适配 model_hybrid.py。
```

二者都是 PyTorch 权重文件，但模型结构不同，不能混用。

### 12.2 是否可以加载原权重继续训练

`main_hybrid.py` 支持：

```bash
--model_path MMpaper_model.pth --load_strict 0
```

含义：

```text
非严格加载旧权重，能匹配的层加载，新增层随机初始化。
```

注意：

- 这只适合调试或迁移初始化；
- 不能作为最终有效结果；
- 最终仍需要训练生成 hybrid checkpoint。

当前训练脚本默认不加载 `MMpaper_model.pth`。如果后续要做迁移初始化，可以在脚本中加：

```bash
--model_path MMpaper_model.pth --load_strict 0
```

## 十三、hybrid 训练后如何评测

假设训练得到：

```text
models/hybrid_DF_weighted_CCE_10_1_stat1_swiglu1_poolcgta_full/best.pth
```

### 13.1 DF 20000 评测

```bash
MODEL_PATH=models/hybrid_DF_weighted_CCE_10_1_stat1_swiglu1_poolcgta_full/best.pth \
  bash scripts/eval_hybrid_df_20000.sh
```

输出：

```text
scores/scores_Hybrid_DF_20000.txt
```

计算 EER：

```bash
python evaluate_2021_DF.py scores/scores_Hybrid_DF_20000.txt ./keys eval
```

### 13.2 In-the-Wild 20000 评测

```bash
MODEL_PATH=models/hybrid_DF_weighted_CCE_10_1_stat1_swiglu1_poolcgta_full/best.pth \
  bash scripts/eval_hybrid_wild_20000.sh
```

输出：

```text
scores/scores_Hybrid_Wild_20000.txt
```

计算 EER：

```bash
python evaluate_in_the_wild.py scores/scores_Hybrid_Wild_20000.txt ./keys eval
```

### 13.3 如果评测的是单模块模型

单模块模型评测时要保持训练时的开关一致。

例如 `train_hybrid_stat_sls.sh` 训练的是：

```text
--use_stat_sls 1
--use_swiglu 0
--pooling_type maxpool
```

评测时需要：

```bash
MODEL_PATH=models/.../best.pth \
USE_STAT_SLS=1 \
USE_SWIGLU=0 \
POOLING_TYPE=maxpool \
bash scripts/eval_hybrid_df_20000.sh
```

完整模型默认是：

```text
USE_STAT_SLS=1
USE_SWIGLU=1
POOLING_TYPE=cgta
```

因此不用额外指定。

## 十四、建议实验路线

如果时间紧，最低展示标准：

1. 原 SLS baseline 使用已有结果；
2. 跑 `hybrid_full` 1 epoch，确认可训练；
3. 跑 `hybrid_full` 3 到 10 epoch；
4. 在 DF 20000 上评测；
5. 时间允许再跑 Wild 20000。

如果要做消融实验：

1. `train_hybrid_stat_sls.sh`
2. `train_hybrid_swiglu.sh`
3. `train_hybrid_temporal.sh`
4. `train_hybrid_full.sh`

每组先：

```bash
EPOCHS=1 BATCH_SIZE=1 bash scripts/train_hybrid_xxx.sh
```

确认能跑后再：

```bash
EPOCHS=10 BATCH_SIZE=1 bash scripts/train_hybrid_xxx.sh
```

最终表格建议包含：

```text
模型名称
是否使用 Mean+Std
是否使用 SwiGLU
是否使用时间注意力
训练 epoch
最佳训练 loss
DF 20000 EER
Wild 20000 EER
备注
```

## 十五、横向扩展功能如何继续开展

扩展功能不依赖 hybrid 训练，可以单独推进。

### 15.1 网页助手

启动：

```bash
bash scripts/run_llm_web.sh
```

如果端口冲突：

```bash
PORT=7861 bash scripts/run_llm_web.sh
```

需要 `.env`：

```text
DEEPSEEK_API_KEY=你的Key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
```

不要把真实 `.env` 上传 GitHub。

### 15.2 风险清单

```bash
bash scripts/risk_wild_20000.sh
```

输出通常在：

```text
reports/
```

### 15.3 视频预处理

```bash
bash scripts/preprocess_video_demo.sh
```

需要准备视频样本。

### 15.4 声纹扩展

先准备独立环境：

```bash
bash scripts/setup_speaker_env.sh
```

再按 `extension_audit.py` 的子命令进行注册和匹配。

## 十六、零训练可解释性如何运行

该模块不改变 `model.py` 和 `main.py`，不需要重新训练。

### 16.1 检查模型中间输出

```bash
python scripts/inspect_model_output.py \
  --audio release_in_the_wild/0.wav \
  --checkpoint MMpaper_model.pth \
  --xlsr-checkpoint xlsr2_300m.pt \
  --device cpu
```

当前已确认：

```text
hidden_states_shape = [1, 24, 201, 1024]
fused_sequence_shape = [1, 201, 1024]
layer_weights_shape = [24]
fake_class_index = 0
```

### 16.2 检查输出一致性

```bash
python scripts/compare_original_output.py \
  --audio release_in_the_wild/0.wav \
  --checkpoint MMpaper_model.pth \
  --xlsr-checkpoint xlsr2_300m.pt \
  --device cpu
```

当前已确认：

```text
max_abs_difference = 0.0
```

### 16.3 生成解释报告

CPU 调试推荐：

```bash
python scripts/explain_audio.py \
  --audio release_in_the_wild/0.wav \
  --checkpoint MMpaper_model.pth \
  --xlsr-checkpoint xlsr2_300m.pt \
  --config configs/explainability_cpu_debug.json \
  --output-dir artifacts/reports \
  --device cpu
```

输出：

```text
artifacts/reports/<audio_name>_evidence.json
artifacts/reports/<audio_name>_report.txt
```

注意：

```text
解释报告只能说明模型输出对遮挡、频带衰减和扰动的敏感性，不能声称确定伪造位置、确定生成器或替代司法鉴定。
```

## 十七、新环境迁移顺序

推荐按这个顺序迁移：

1. 从 GitHub clone 项目；
2. 创建 `venv`；
3. 安装 PyTorch、requirements、fairseq；
4. 放入 `xlsr2_300m.pt`；
5. 放入 `MMpaper_model.pth`；
6. 放入 DF / Wild / 训练数据；
7. 跑环境检查；
8. 跑 `eval_df_tiny10.sh`；
9. 跑 `eval_df_20000.sh` 和 `eval_wild_20000.sh`；
10. 跑 `test_hybrid_modules.sh`；
11. 跑 `EPOCHS=1 bash scripts/train_hybrid_full.sh`；
12. 正式训练；
13. 评测 hybrid checkpoint；
14. 更新结果文档；
15. 上传 GitHub。

## 十八、上传 GitHub 前检查

上传前检查：

```bash
git status -sb
```

不应上传：

```text
.env
venv/
venv_speaker/
MMpaper_model.pth
xlsr2_300m.pt
data/
release_in_the_wild/
samples/
大体积训练 checkpoint
```

可以上传：

```text
代码
脚本
小型协议文件
key/metadata 子集
score 文本结果
报告文档
```

如果训练 checkpoint 很大，建议：

- 不上传普通 GitHub；
- 使用网盘或 GitHub Release；
- 或只上传结果 score 和报告；
- 文档中写明 checkpoint 获取方式。

## 十九、当前本地工作区注意事项

当前项目曾出现一个本地删除状态：

```text
fairseq-a54021305d6b3c4c5959ac9395135f63202db8f1.zip
```

该删除不是 hybrid 实验的一部分。提交前要确认是否真的要删除。如果不想删除，应恢复该文件或避免把删除状态提交。

当前新增的实验文件属于本轮 hybrid 改动，应当作为一个独立提交更清晰。

## 二十、给新对话的最短上下文

如果以后开新对话，可以把下面这段直接给新的助手：

```text
这是一个基于 SLSforASVspoof-2021-DF 的音频深度伪造检测项目。原主线 model.py/main.py 使用原始 MMpaper_model.pth，已经完成 DF 20000 EER=1.99%、In-the-Wild 20000 EER≈7.47%。项目新增了横向扩展功能，包括网页助手、风险报告、FFmpeg、Whisper、WeSpeaker、VLM、时间戳等。

现在又新增了 hybrid 创新实验线，但不覆盖原主线：model_hybrid.py/main_hybrid.py。hybrid 包含 Mean+Std StatisticalSLS、SwiGLU、TemporalAttentionPooling、ChannelGuidedTemporalAttention。训练脚本在 scripts/train_hybrid_*.sh，评测脚本在 scripts/eval_hybrid_*.sh。hybrid 新增层不能直接用 MMpaper_model.pth 作为最终权重，必须重新训练得到 models/.../best.pth，再用 MODEL_PATH=... bash scripts/eval_hybrid_df_20000.sh 或 eval_hybrid_wild_20000.sh 评测。另有零训练可解释性模块 explainability/ 和 scripts/explain_audio.py，可在不训练的情况下生成 evidence.json 和中文模板报告。

原主线要保持稳定，不要改 model.py/main.py。若不训练 hybrid，继续使用 scripts/eval_df_20000.sh 和 scripts/eval_wild_20000.sh。复原方案见 创新实验复原文档.md，详细实现状态见 实现说明.md。
```

## 二十一、推荐下一步

当前最推荐的下一步不是直接长时间训练，而是：

```bash
source venv/bin/activate
bash scripts/test_hybrid_modules.sh
EPOCHS=1 BATCH_SIZE=1 bash scripts/train_hybrid_full.sh
```

如果能正常完成，再跑：

```bash
EPOCHS=3 BATCH_SIZE=1 bash scripts/train_hybrid_full.sh
```

确认 loss 下降后，再决定是否跑四组消融实验。
